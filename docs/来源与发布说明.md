# 来源、排除项与发布变动

[返回 README](../README.md) · [逐文件来源 JSON](../inventory/source-manifest.json) · [脚本索引](../inventory/脚本索引.md)

## 来源与处理范围

源目录 `/home/unitree/jumpserver-handoff-20260913` 全程只读，完整交接包留本机。依据总交接说明、三份接入/部署手册、28 本地脚本和镜像清单重新编写中文文档；不把原历史页面/部署记录当作本次实机验证。

源文件共 193 个：107 个原样保留，4 个修改（3 个源码脱敏和 1 个中文索引重写），82 个未发布。新增文档、示例、清单和截图另行记录于仓库，源文件映射不将其伪装成历史原件。

## 代码字节保留与必要变动

28 个本地脚本全部保留，26 个与原件字节一致；两个工具也字节一致。每个源文件与发布文件的 SHA-256 见 JSON，可据此检查未变字节。以下三项源码作必要修改；另将 `inventory/脚本索引.md` 重写为中文发布索引，不涉及业务逻辑：

| 文件 | 修改 | 原因 |
|---|---|---|
| `code/local/diagnose-jumpserver-import.py` | 删除固定个人名单，运行时输入目标用户名，空名单退出 | 避免发布真实人员清单 |
| `code/local/diagnose-jumpserver-operator-login.py` | 删除真实名单和固定初始口令；交互输入名单和 getpass 口令，JSON 转义后经容器标准输入传递；空输入退出 | 避免发布或通过进程参数暴露口令；保留查询字段和只读密码核对逻辑 |
| `upstream/jumpserver-installer-v4.10.19/scripts/4_install_jumpserver.sh` | 将安装结束时的默认密码展示行替换为安全初始化提示 | 仓库不提供固定初始口令；未改变安装过程或上游许可 |

未修改业务部署、桌面、防火墙、镜像加载和权限检查逻辑。输入脱敏变动只经静态审阅，按要求未执行测试。原 IP、主机名、系统账号、路径、UUID 不是凭据，保留于历史专用代码以维持守卫和来源；新文档使用示例目标并指明配置位置。

## 排除项与原因

| 原内容 | 处理与原因 |
|---|---|
| `users/` 全部 CSV | 不发布真实用户、姓名、邮箱或导入数据；文件名写 NO-CREDENTIALS 也不是发布许可 |
| `snapshots/` 原 zip、采集报告和文件树 | 不发布未经完全审查的实机内容、个人信息、重复脚本；必要配置原理由源码与新文档说明 |
| 原顶层总说明及 `docs/` 历史文档/证书/脚本 | 不原样复制可能含凭据和旧状态的记录；中文主题文档为重新整理，完整原件仍留本机 |
| 七个 `offline/images/*.tar` | 大体积运行物不入 Git；完整名称、digest、尺寸、SHA-256 与下载/导入说明已保留 |
| `offline/bin/crane` | 第三方二进制留本机，按来源取得与准备主机匹配版本 |
| `upstream/*.tar.gz`、`*.zip` | 原安装器 tar、Core 源码包、旧 edge 工具包留本机；避免冗余和未审阅解包内容 |
| 安装器 `.github/` | 上游 CI、Issue 和发布自动化不适用于本交付仓库 |
| 原 `SHA256SUMS`、原 inventory | 与旧全集绑定，改为本次发布来源清单，不沿用旧验证报告 |

没有复制 live config、私钥、数据库、录像、运行日志或 `.env`。没有创建用于公开的真实用户 CSV，也没有新增整体开源许可。没有为本次整理解包任何源 tar/zip。

## 上游和许可

安装器来自源包已经展开的 `jumpserver-installer-v4.10.19`，保留其完整运行源码、配置模板、本地化资源和 [GPL-3.0 LICENSE](../upstream/jumpserver-installer-v4.10.19/LICENSE)；只排除仓库专属 `.github` 配置并替换一行密码展示提示。其他本地脚本原包没有统一许可，本仓库不代为授予。Core/Koko/Web/Lion/PostgreSQL/Redis/执行镜像各遵循其上游许可，镜像未随 Git 分发。

## 对原状态的准确表述

历史交接包并非完整数据库/整机恢复备份；部分 root 文件及设备实时状态缺失。本次仅做源码发布检查，网页截图只展示页面，不连接资产。后续验收要求见 [限制与验证边界](限制与验证边界.md)。
